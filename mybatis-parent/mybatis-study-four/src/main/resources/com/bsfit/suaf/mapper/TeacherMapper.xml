<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--mybatis核心配置文件，名字随便起-->
<mapper namespace="com.bsfit.suaf.mapper.TeacherMapper">

    <resultMap id="teacherStudent" type="Teacher">
        <result property="id" column="tid"/>
        <result property="name" column="tname"/>
        <!--因为复杂的属性单独处理，对象用association,集合用collection-->
        <!--javaType使用指定的属性类型，ofType用来获取集合中的泛型信息-->
        <collection property="students" ofType="Student">
            <result property="id" column="sid"/>
            <result property="name" column="sname"/>
        </collection>
    </resultMap>
    <select id="getTeacherOne" resultMap="teacherStudent">
        select t.id tid,t.name tname,s.id sid,s.name sname from teacher t,student s where s.tid = t.id and t.id = #{id};
    </select>

<!--============================================================================================-->

    <resultMap id="teacherStudent2" type="Teacher">
        <!--前边一样的可省略-->
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <!--column是老师的id，传递出来的-->
        <collection property="students" column="id" javaType="ArrayList" ofType="Student" select="selectStudent"/>
    </resultMap>
    <select id="getTeacherOne2" resultMap="teacherStudent2">
        select * from teacher where id = #{id};
    </select>
    <select id="selectStudent" resultType="Student">
        select * from student where tid = #{随便写，因为只有一个参数，占位符而已};
    </select>
</mapper>
